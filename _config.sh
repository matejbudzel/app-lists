#!/bin/bash

# Centralized configuration for app-lists scripts
# OUTDIR: destination directory for app lists
# Can be overridden by exporting OUTDIR before invoking scripts.
OUTDIR="${OUTDIR:-${HOME}/.applists}"
